#include "main.h"
#include <stdio.h>
/**
 * _abs - compute absolute value
 * return:absolute value of integer
 *@n: the integer to check
 */

 int _abs(int n)
 {
 	if (n >= 0)
	{
		return (n);
	}
	else 
		return (-n);
}
